@startuml Brand Management Activity Diagram
!theme plain
skinparam backgroundColor #FFFFFF
skinparam activityFontSize 12
skinparam activityFontColor #333333
skinparam activityBorderColor #666666
skinparam activityBackgroundColor #F0F0F0
skinparam activityDiamondBackgroundColor #FFE6E6
skinparam activityDiamondBorderColor #CC0000

Brand Brand Management Activity: execute the validated workflow

start

:Brand Brand Management Activity: execute the validated workflow;

if (condition?) then (Yes)
  :Brand Brand Management Activity: execute the validated workflow;
  :Brand Brand Management Activity: execute the validated workflow;
  :Brand Brand Management Activity: execute the validated workflow;
  note right
  Brand Brand Management Activity: execute the validated workflow
  • WHERE deletedAt IS NULL
  • Include brandTranslations
  • Language-specific translations
  • ORDER BY createdAt DESC
  • Brand Brand Management Activity: execute the validated workflow
  end note
  :Brand Brand Management Activity: execute the validated workflow;
  :Brand Brand Management Activity: execute the validated workflow;
  :Brand Brand Management Activity: execute the validated workflow;

else (No)
  :Brand Brand Management Activity: execute the validated workflow;
  :Brand Brand Management Activity: execute the validated workflow;
  :Brand Brand Management Activity: execute the validated workflow;
  if (condition?) then (Yes)
    :Brand Brand Management Activity: execute the validated workflow;
    :Brand Brand Management Activity: execute the validated workflow;
    :Brand Brand Management Activity: execute the validated workflow;
  else (No)
    :Brand Brand Management Activity: execute the validated workflow;
  endif

else (No)
  :Brand Brand Management Activity: execute the validated workflow;
  :Brand Brand Management Activity: execute the validated workflow;
  :Brand Brand Management Activity: execute the validated workflow;
  :Brand Brand Management Activity: execute the validated workflow;
  if (condition?) then (Yes)
    :Brand Brand Management Activity: execute the validated workflow;
    :Brand Brand Management Activity: execute the validated workflow;
    :Include brandTranslations;
    :Brand Brand Management Activity: execute the validated workflow;
  else (No)
    :Brand Brand Management Activity: execute the validated workflow;
  endif

else (No)
  :Brand Brand Management Activity: execute the validated workflow;
  :Brand Brand Management Activity: execute the validated workflow;
  :Brand Brand Management Activity: execute the validated workflow;
  :Brand Brand Management Activity: execute the validated workflow;
  :Brand Brand Management Activity: execute the validated workflow;
  if (condition?) then (Yes)
    :Brand Brand Management Activity: execute the validated workflow;
    :Brand Brand Management Activity: execute the validated workflow;
    :Include brandTranslations;
    :Brand Brand Management Activity: execute the validated workflow;
  else (No)
    :Brand Brand Management Activity: execute the validated workflow;
  endif

else (No)
  :Brand Brand Management Activity: execute the validated workflow;
  :Brand Brand Management Activity: execute the validated workflow;
  :Brand Brand Management Activity: execute the validated workflow;
  :Brand Brand Management Activity: execute the validated workflow;
  :Brand Brand Management Activity: execute the validated workflow;
  :Brand Brand Management Activity: execute the validated workflow;
  :Brand Brand Management Activity: execute the validated workflow;

else (No)
  :Brand Brand Management Activity: execute the validated workflow;
  :Brand Brand Management Activity: execute the validated workflow;
  :Brand Brand Management Activity: execute the validated workflow;
  :Brand Brand Management Activity: execute the validated workflow;
  if (condition?) then (Yes)
    :Brand Brand Management Activity: execute the validated workflow;
    if (condition?) then (Yes)
      :Brand Brand Management Activity: execute the validated workflow;
    else (No)
      :Brand Brand Management Activity: execute the validated workflow;
      :Brand Brand Management Activity: execute the validated workflow;
      :Brand Brand Management Activity: execute the validated workflow;
    endif
  else (No)
    :Brand Brand Management Activity: execute the validated workflow;
  endif

else (No)
  :Brand Brand Management Activity: execute the validated workflow;
  :Brand Brand Management Activity: execute the validated workflow;
  :Brand Brand Management Activity: execute the validated workflow;
  :Brand Brand Management Activity: execute the validated workflow;
  :Brand Brand Management Activity: execute the validated workflow;
  if (condition?) then (Yes)
    :Brand Brand Management Activity: execute the validated workflow;
    :Brand Brand Management Activity: execute the validated workflow;
    :Brand Brand Management Activity: execute the validated workflow;
  else (No)
    :Brand Brand Management Activity: execute the validated workflow;
  endif

else (No)
  :Brand Brand Management Activity: execute the validated workflow;
  :Brand Brand Management Activity: execute the validated workflow;
  :Brand Brand Management Activity: execute the validated workflow;
  :Brand Brand Management Activity: execute the validated workflow;
  :Brand Brand Management Activity: execute the validated workflow;
  :Brand Brand Management Activity: execute the validated workflow;
  :Brand Brand Management Activity: execute the validated workflow;

else (No)
  :Brand Brand Management Activity: execute the validated workflow;
endif

:Brand Brand Management Activity: execute the validated workflow;
:Brand Brand Management Activity: execute the validated workflow;

stop

@enduml
