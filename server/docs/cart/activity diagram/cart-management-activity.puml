@startuml Cart Management Activity Diagram
!theme plain
skinparam backgroundColor #FFFFFF
skinparam activityFontSize 12
skinparam activityFontColor #333333
skinparam activityBorderColor #666666
skinparam activityBackgroundColor #F0F0F0
skinparam activityDiamondBackgroundColor #FFE6E6
skinparam activityDiamondBorderColor #CC0000

Cart Cart Management Activity: execute the validated workflow

start

:Cart Cart Management Activity: execute the validated workflow;

if (condition?) then (Yes)
  :Cart Cart Management Activity: execute the validated workflow;
  :Cart Cart Management Activity: execute the validated workflow;
  :Cart Cart Management Activity: execute the validated workflow;
  note right
  Cart Cart Management Activity: execute the validated workflow
  • JOIN CartItem → SKU → Product → ProductTranslation
  • GROUP BY Product.createdById (shop)
  • Cart Cart Management Activity: execute the validated workflow
  • Cart Cart Management Activity: execute the validated workflow
  • Cart Cart Management Activity: execute the validated workflow
  end note
  :Cart Cart Management Activity: execute the validated workflow;
  :Cart Cart Management Activity: execute the validated workflow;
  :Cart Cart Management Activity: execute the validated workflow;
  :Cart Cart Management Activity: execute the validated workflow;

else (No)
  :Cart Cart Management Activity: execute the validated workflow;
  :Cart Cart Management Activity: execute the validated workflow;
  :Cart Cart Management Activity: execute the validated workflow;
  if (condition?) then (Yes)
    :Cart Cart Management Activity: execute the validated workflow;
    if (condition?) then (Yes)
      :Cart Cart Management Activity: execute the validated workflow;
      if (condition?) then (Yes)
        :Cart Cart Management Activity: execute the validated workflow;
        if (condition?) then (Yes)
          :Cart Cart Management Activity: execute the validated workflow;
          :Cart Cart Management Activity: execute the validated workflow;
        else (No)
          :Cart Cart Management Activity: execute the validated workflow;
          :Cart Cart Management Activity: execute the validated workflow;
        endif
        :Cart Cart Management Activity: execute the validated workflow;
        note right
        Logic Upsert:
        • Cart Cart Management Activity: execute the validated workflow
        • Cart Cart Management Activity: execute the validated workflow
        • Unique constraint: userId + skuId
        end note
        :Cart Cart Management Activity: execute the validated workflow;
      else (No)
        :Cart Cart Management Activity: execute the validated workflow;
      endif
    else (No)
      :Cart Cart Management Activity: execute the validated workflow;
    endif
  else (No)
    :Cart Cart Management Activity: execute the validated workflow;
  endif

else (No)
  :Cart Cart Management Activity: execute the validated workflow;
  :Cart Cart Management Activity: execute the validated workflow;
  if (condition?) then (Yes)
    :Cart Cart Management Activity: execute the validated workflow;
    if (condition?) then (Yes)
      :Cart Cart Management Activity: execute the validated workflow;
      if (condition?) then (Yes)
        :Cart Cart Management Activity: execute the validated workflow;
        :Cart Cart Management Activity: execute the validated workflow;
        :Cart Cart Management Activity: execute the validated workflow;
        :Cart Cart Management Activity: execute the validated workflow;
      else (No)
        :Cart Cart Management Activity: execute the validated workflow;
      endif
    else (No)
      :Cart Cart Management Activity: execute the validated workflow;
    endif
  else (No)
    :Cart Cart Management Activity: execute the validated workflow;
  endif

else (No)
  :Cart Cart Management Activity: execute the validated workflow;
  :Cart Cart Management Activity: execute the validated workflow;
  :Cart Cart Management Activity: execute the validated workflow;
  if (condition?) then (Yes)
    :Cart Cart Management Activity: execute the validated workflow;
    :Cart Cart Management Activity: execute the validated workflow;
    :Cart Cart Management Activity: execute the validated workflow;
  else (No)
    :Cart Cart Management Activity: execute the validated workflow;
  endif

else (No)
  :Cart Cart Management Activity: execute the validated workflow;
endif

:Cart Cart Management Activity: execute the validated workflow;
:Cart Cart Management Activity: execute the validated workflow;

stop

@enduml
