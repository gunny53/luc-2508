@startuml Category - Update Activity
!theme plain
skinparam backgroundColor #FFFFFF
skinparam activityFontSize 12
skinparam activityFontColor #333333
skinparam activityBorderColor #666666
skinparam activityBackgroundColor #F7F7F7

Category Category Update Activity: execute the validated workflow

start
:Category Category Update Activity: execute the validated workflow;
:category.update({ where: id & deletedAt=null, data: {..., updatedById} }) include translations;
if (condition?) then (Yes)
  :Build message i18n;
  :Category Category Update Activity: execute the validated workflow;
else (No)
  :Category Category Update Activity: execute the validated workflow;
endif
stop

@enduml


